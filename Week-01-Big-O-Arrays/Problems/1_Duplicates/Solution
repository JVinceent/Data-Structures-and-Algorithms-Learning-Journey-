#Brute Force - By traversing, i searched for each elements if they have duplicates. This will result to O(n^2) since i searched for nth times

class Solution:
    def hasDuplicate(self, nums: List[int]) -> bool:
        for i in range(len(nums)):
            for j in range(i+1, len(nums)):
                if nums[i] == nums[j]:
                    return True
        return False

#sorting  - By using sort, i was able to make the duplicate side by side. Thus, finding it would be optomal than traversing each element

class Solution:
    def hasDuplicate(self, nums: List[int]) -> bool:
        nums.sort()
        for i in range(1, len(nums)):
            if nums[i] == nums[i - 1]:
                return True
        return False

# HashSet - Storing new elemets while denying duplicates. The complexity is O(1)
class Solution:
    def hasDuplicate(self, nums: List[int]) -> bool:
        hashset = set()
        for i in nums:
            if i in hashset:
                return True
            hashset.add(i)
        return False
